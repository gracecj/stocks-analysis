name: Update Quarto Project

on:
  schedule:
    - cron: '50 1 * * *'  # Run the workflow every day at 1:50 AM UTC

jobs:
  test-schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Every time
        run: echo "This step will always run"
